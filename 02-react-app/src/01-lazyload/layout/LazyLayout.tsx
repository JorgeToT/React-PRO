export const LazyLayout = () => {
    return (
        <div>
            <h1>Lazy Layout</h1>
            <p>
                This layout is lazy-loaded. It is not loaded until the user
                navigates to a route that uses it.
            </p>
        </div>
    );
};
